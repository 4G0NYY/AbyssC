[package]
name = "orchestrator"
version = "0.1.0"
edition = "2024"

[dependencies]
archive_engine = { path = "../archive_engine" }
# For beautiful CLI parsing
clap = { version = "4.6.1", features = ["derive"] }